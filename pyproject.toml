[project]
name = "arcter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "platformdirs>=4.5.1",
    "pycountry>=24.6.1",
    "pydantic>=2.12.5",
    "pydantic-extra-types>=2.11.0",
    "typer>=0.23.0",
]

[project.scripts]
arcter = "arcter.cli:app"

[tool.uv]
package = true
